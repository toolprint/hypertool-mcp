/**
 * Toolset filtering utilities
 */

import { DiscoveredTool } from "../discovery/types";
import { ToolsetConfig } from "./types";

// Note: This module is kept minimal as filtering is now handled by 
// ToolsetManager.applyConfig() using the discovery engine.